<%= form_for(charge) do |f| %>
  <% if charge.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(charge.errors.count, "error") %> prohibited this charge from being saved:</h2>

      <ul>
      <% charge.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :summary %>
    <%= f.text_field :summary %>
  </div>

  <div class="field">
    <%= f.label :start_date %>
    <%= f.date_select :start_date %>
  </div>

  <div class="field">
    <%= f.label :end_date %>
    <%= f.date_select :end_date %>
  </div>

  <div class="field">
    <%= f.label :is_current %>
    <%= f.check_box :is_current %>
  </div>

  <div class="field">
    <%= f.label :enterprise_id %>
    <%= f.text_field :enterprise_id %>
  </div>

  <div class="field">
    <%= f.label :org_level %>
    <%= f.text_field :org_level %>
  </div>

  <div class="field">
    <%= f.label :market_responsability %>
    <%= f.text_field :market_responsability %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
