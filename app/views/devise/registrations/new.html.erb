<div class="container">
  <div class="col-xs-12 col-sm-4 col-md-4 col-lg-4">
    <div class="tg-loginarea">
      <br>
        <div class="tg-bordertitle">
        <h3>Registrate gratis</h3>
      </div>
        
        <fieldset>

      <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        
        <%= f.email_field :email, autofocus: true,placeholder:"Email" %>
      </div>

      <div class="form-group">
        <!-- <%= f.label :password %> -->
        <% if @minimum_password_length %>
        <em>(<%= @minimum_password_length %> characters minimum)</em>
        <% end %><br />
        <%= f.password_field :password, autocomplete: "off",placeholder:"Ingresa contraseña" %>
      </div>

      <div class="form-group">
        <!-- <%= f.label :password_confirmation %><br /> -->
        <%= f.password_field :password_confirmation, autocomplete: "off",placeholder:"Repite contraseña" %>
      </div>

 <!--      <div class="actions">
        <%= f.submit "Sign up" %>
      </div> -->

        <div class="form-group">
        <!-- <%= f.submit "Ingresa ahora" ,:class=>'tg-btn tg-btn-lg'  %> -->
        <button class="tg-btn tg-btn-lg" type="submit">Registrarme</button>
      </div>

      <% end %>

      <%= render "devise/shared/links" %>

        </fieldset>
        <br>
    </div>
  </div>
</div> 